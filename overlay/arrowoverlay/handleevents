#include "arrowoverlay.ih"

    // by 

void ArrowOverlay::handleEvents(bool &running, SDL_Event &e) {
{
    while (SDL_PollEvent(&e)) 
    {
        if (e.type == SDL_QUIT)
            running = false;
    }
}
